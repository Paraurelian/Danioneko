# oneko.js

A hacky script I DID NOT write to put a DANI DORIAN on my DISCORD.

The default image is `oneko.gif` in the same directory as the script. This can be changed by adding `data-cat="yourimage.png"` to your `<script>` tag. (good to know thank u)

demo: https://adryd.com
